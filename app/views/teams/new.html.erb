<%= form_for @team do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
    <%= render partial: "error_for_field", locals: { model: @team, field: :name } %>

  <%= f.label :project %>
  <%= collection_check_boxes(:team, :project_ids, Project.all, :id, :name) %>

  <%= f.submit "Create Team" %>
<% end %>

